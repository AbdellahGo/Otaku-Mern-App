export type ArgTypes = {
  title: string,
  type: string,
  releaseDate: string,
  status: string,
  chapters: string,
  author: string,
  description: string,
  imageUrl: string,
  sourceUrl: string,
  lastWatch: string,
  selectedGenres: string[],
}